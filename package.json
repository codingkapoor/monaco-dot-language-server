{
  "name": "monaco-dot-language-server",
  "version": "1.0.0",
  "description": "A Monaco editor language server protocol implementation for DOT language"
  "scripts": {
    "postinstall": "cd parser && npm install && cd ../server && npm install && cd ../client && npm install"
  },
  "author": "codingkapoor",
  "license": "MIT"
}
